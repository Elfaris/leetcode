#include <iostream>
using namespace std;
int  open (int n,int sum){
    if(n !=0) {
        sum += n % 10;
        n = (n - n % 10)/10;
        open(n, sum);
    }
    else if(n+sum > 9)
        open(n+sum, 0);
}
int main() {
    int num = 0;
    num = open(19 ,0);
    cout<<num;
}
